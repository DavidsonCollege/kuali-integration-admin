# Default Kuali Build base URL shown on the connect form.
# The user can override this when they connect; this only sets the initial value.
NUXT_PUBLIC_DEFAULT_BASE_URL=https://YOUR_INSTITUTION.kualibuild.com
